import { RolaColorPipe } from './rola-color.pipe';

describe('RolaColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RolaColorPipe();
    expect(pipe).toBeTruthy();
  });
});
